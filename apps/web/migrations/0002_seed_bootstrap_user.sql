-- ログイン (OAuth / セッション) はまだ無いため、単一の暫定ユーザーを固定 ID で作っておく。
-- 設定画面の API キー発行はこのユーザーに対して行う。ID を固定しておかないと
-- マイグレーションを貼り直すたびに既存の鍵が別ユーザーを指してしまうため、値は変更しないこと。
-- TODO: 認証を実装したら、このシードは削除して実ユーザーに移行する。
INSERT INTO users (id, display_name, created_at)
VALUES ('01900000-0000-7000-8000-000000000001', 'dragonfly', 0)
ON CONFLICT(id) DO NOTHING;
